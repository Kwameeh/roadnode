# Prototype 1 — Pi Zero 2 W

The Zero 2 W runs Raspberry Pi OS headlessly. It handles python-OBD, USB/Bluetooth, GPS UART, I2C sensors, MQTT, the internal engine API and the LAN web server.

Removing the graphical desktop/touchscreen reduces RAM usage and leaves browser rendering to the user's phone/laptop.
