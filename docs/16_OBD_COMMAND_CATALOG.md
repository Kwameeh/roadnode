# OBD Command Catalog

Run:

```bash
telemetry obd-catalog
```

The command reads the catalog from the installed python-OBD package rather than duplicating its command table. python-OBD includes current data, freeze-frame related commands, DTC operations, current-cycle DTCs and vehicle information such as VIN.
