# Future System

Future improvements include local authentication, fallback Wi-Fi access-point setup, offline SQLite telemetry queue, cellular connectivity, direct CAN, manufacturer-specific command packs, richer trip analytics, secure device provisioning and custom automotive hardware.
