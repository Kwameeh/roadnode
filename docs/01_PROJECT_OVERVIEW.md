# Project Overview

The system collects OBD-II vehicle data, VIN/diagnostics, GPS, motion/driver-behaviour information and device health. The Pi is headless: a phone or computer on the same network opens the local web application. Telemetry can also be sent to a cloud backend with MQTT.

The Raspberry Pi Zero 2 W is Prototype 1, not necessarily the final production processor.
